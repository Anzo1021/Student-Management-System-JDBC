# Student Management System (JDBC + MySQL)

A console-based Java application to manage student records using JDBC connectivity with MySQL.

## Features
- Add, View, Update, and Delete students
- Auto grade calculation based on marks
- Uses JDBC `PreparedStatement` and `ResultSet`
- Console menu-based interface

## Technologies Used
- Java
- MySQL
- JDBC
- IntelliJ IDEA
